version: "3.7"

services:
  web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .env:/.env
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
    volumes:
      - "redis-data:/data"

volumes:
  redis-data:
